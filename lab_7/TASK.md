1. Оголосіть та ініціалізуйте словник у різний спосіб.
```python
x = {1:1, 2:2}
y = dict([(1,1), (2,2)])
z = dict()
z[1] = 1
z[2] = 2
```

2. Додайте ще кілька пар «ключ: значення» до наступного словника: {0: 10, 1: 20}.
```python
x = {0: 10, 1: 20}
x[2] = 30
x[3] = 40
```

3. Напишіть скрипт, який із трьох словників створить один новий.
```python
x = {1:1}
y = {2:2}
z = {3:3}
merged = x | y | z
```

4. Напишіть скрипт, який перевіряє, чи існує заданий ключ у словнику.
```python
x = {1:1}
inp = int(input("Enter key name: "))
print(inp in x)
```

5. Напишіть скрипт, щоб видалити елемент словника.
```python
x = {1:1}
inp = int(input("Enter key name: "))
if inp in x:
    x.pop(inp)
```

6. Напишіть скрипт, який виводить максимальне та мінімальне числа серед значень словника.
```python
x = {k: k*10 for k in range(10)}
print(max(x.values()))
print(min(x.values()))
```

7. Елемент у словнику має дві частини. Як вони називаються?
> key and value

8. Яка частина елемента словника має бути немутованою?
> key

9. Припустимо, що 'старт': 1472 є елементом словника. Що є ключем? І що значення?
> 'старт' -> key
> 1472 -> value

10. Припустимо, що створено словник з ім'ям працівника. Що робить наведена нижче інструкція?
employee['id'] = 54321
> Создает новую пару внутри словаря `employee`

11. Що покаже наведений нижче фрагмент коду?
stuff = {1: 'ааа', 2: 'ббб', 3: 'ввв'}
print(stuff[3])
> 'ввв'

12. Як визначити, чи існує пара "ключ: значення" у словнику?
> `<key> in <dict> -> bool`

13. Що покаже наведений нижче фрагмент коду?
stuff = { 1 :'а а а ', 2:'ббб', 3:'ввв')
print(len(stuff))
> Syntax error

14. Що покаже наведений нижче фрагмент коду?
stuff = [1: 'ааа', 2: 'ббб', 3: 'ввв'}
for k in stuff:
> Syntax error

15. У чому різниця між словниковими методами pop() та popitem()?
> .pop() -> требует аргуменент ключа .popitem() -> удаляет последнюю вставленную пару

16. Що повертає метод items()?
> Объект dict_items с парами ключ-значение

17. Що повертає метод keys()?
> Объект dict_keys с ключами

18. Що повертає метод values()?
> Объект dict_values со значениями

19. Припустимо, що є такий перелік:
names = ['Кріс', 'Кеті', 'Джоанна', 'Курт']
Напишіть інструкцію з використанням включення до словника для створення словника, в якому кожен елемент містить ім'я зі списку names як ключ і довжину цього імені як значення.
```python
names = ['Кріс', 'Кеті', 'Джоанна', 'Курт']
x = {k: len(k) for k in names}
```

20. Припустимо, що є такий словник:
phonebook = {'Кріс':'919-555-1111', 'Кеті':'828-555-2222', 'Джоанна': '704-555-3333', 'Курт':'919-555-3333' }
Напишіть інструкцію з використанням включення до словника для створення другого словника, що містить елементи телефонної книги phonebook, які мають значення, що починається з '919'.
```python
phonebook = {'Кріс':'919-555-1111', 'Кеті':'828-555-2222', 'Джоанна': '704-555-3333', 'Курт':'919-555-3333' }
filtered = {k: v for k, v in phonebook.items() if v.startswith("919")}
```
